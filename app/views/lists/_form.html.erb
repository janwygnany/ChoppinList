<%= form_with(model: list, local: true) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :list_name %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :list_quantity %>
  </div>

  <div class="field">
    <%= form.label :place %>
    <%= form.text_field :place, id: :list_place %>
  </div>

  <div class="field">
    <%= form.label :who %>
    <%= form.text_field :who, id: :list_who %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
